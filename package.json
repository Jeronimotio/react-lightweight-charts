{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "start": "lerna bootstrap && lerna run compile && lerna run start --stream"
  }
}
